# Python packages used by the pipeline. Tested with the versions on the right
# (Python 3.12.3); current releases are expected to work.
#   pip install -r demo/requirements.txt
duckdb>=1.1        # tested 1.5.1
polars>=1.0        # tested 1.39.3
pandas>=2.1        # tested 3.0.2 (DataFrame.map is used, so pandas >= 2.1)
numpy>=1.26        # tested 2.2.6
matplotlib>=3.8
pyarrow>=15         # tested 23.0.1; DuckDB converts query results to polars through Arrow
jinja2>=3           # tested 3.1.6; pandas DataFrame.to_latex needs it
